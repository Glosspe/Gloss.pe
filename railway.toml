[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh start.sh"
healthcheckPath = "/api/products/search?category=Todos"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
